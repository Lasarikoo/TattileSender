# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tattile_sender
DB_USER=tattile
DB_PASSWORD=changeme

# Directorio de certificados (en el servidor)
CERTS_DIR=/etc/tattile_sender/certs

# Puerto de recepción de lecturas Tattile
TRANSIT_PORT=33334

# Entorno (dev, prod, etc.)
APP_ENV=dev

# Notas:
# - En desarrollo copia este archivo a .env y ajusta los valores según tu equipo.
# - En producción, las variables se inyectarán desde el entorno del sistema o
#   mediante ficheros seguros gestionados fuera del repositorio.
