# Valores de ejemplo orientados a producción. Cambia TODO antes de usar en un VPS real.
APP_ENV=prod

DB_HOST=localhost
DB_PORT=5432
DB_NAME=tattile_sender_prod
DB_USER=tattile_prod
DB_PASSWORD=cambia_est0

# Ruta segura donde se almacenan los certificados PFX en el servidor.
CERTS_DIR=/etc/tattile_sender/certs

# Puerto en el que el servicio de ingesta recibirá las lecturas Tattile.
TRANSIT_PORT=33334

# Notas:
# - Sustituye las credenciales y rutas por valores reales y seguros.
# - CERTS_DIR debe apuntar a un directorio protegido en el VPS donde se montarán los .pfx.
# - APP_ENV=prod prepara la configuración para ejecución en producción; cámbialo solo si necesitas otro entorno.
